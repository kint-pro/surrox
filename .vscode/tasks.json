{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy Docs",
      "type": "shell",
      "command": "./deploy.sh",
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "presentation": { "reveal": "always", "panel": "shared" }
    }
  ]
}
